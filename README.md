# Docker Task App

Small browser-based task app containerized with Docker.

This repository continues the project beyond the earlier lab-specific stages and is used for the next steps such as:

- Docker Compose
- Volumes
- further container workflow improvements